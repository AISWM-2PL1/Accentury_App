import SwiftUI

/// 앱 진입점. 안드로이드의 `MainActivity` + `AccenturyApplication` 자리다.
@main
struct AccenturyApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
