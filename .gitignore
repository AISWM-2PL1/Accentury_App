*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# === 공통 (모든 파트) ===
.env
.env.*
.omc/
.idea/
.vscode/

# 자산 빌드 스크립트(assets/*/build.py)는 서로를 모듈로 읽는다 (KAN-179). 읽히는 쪽은
# sys.dont_write_bytecode로 막지만 읽는 파일 자신의 .pyc는 그 설정보다 먼저 쓰인다.
__pycache__/

# === iOS (KAN-108) ===
# 개발자 개인 설정 파일. 팀 ID와 실기기 확인용 터널 주소가 들어가므로 커밋 대상이 아니다
# (안드로이드의 local.properties와 같은 자리). 템플릿은 Local.xcconfig.example로 커밋돼 있다.
ios/Accentury/Config/Local.xcconfig
# Xcode가 사람마다 다르게 쓰는 상태(열린 파일, 브레이크포인트, 스킴 선택).
# Accentury.xcodeproj 자체는 커밋한다 - 원본은 ios/project.yml이고 이건 xcodegen 산출물이다.
ios/**/xcuserdata/
ios/**/*.xcuserstate
# SwiftPM 빌드 캐시와 로컬 패키지 상태.
ios/**/.build/
ios/**/.swiftpm/
ios/DerivedData/
# xcodebuild -resultBundlePath 산출물. CI(test.yml의 ios-test)는 실패했을 때만 아티팩트로
# 올리고, 로컬에서 같은 명령을 돌리면 여기 남는다.
ios/TestResults.xcresult/
# Package.resolved는 일부러 빼지 않는다 - 외부 의존성이 생기면 버전을 고정하는 파일이다.

# === Android 릴리스 서명 (KAN-163) ===
# 릴리스 키스토어. 이 파일 하나를 잃으면 스토어에 올라간 앱의 업데이트를 영원히 올릴 수 없다
# (Play App Signing에 등록해 뒀다면 업로드 키 재발급으로 살릴 수 있지만 그건 등록해 둔 경우다).
# 레포가 아니라 팀이 정한 안전한 곳에 두고, 보관 위치는 위키에 적는다.
# CI는 이 파일을 base64 시크릿(RELEASE_KEYSTORE_BASE64)으로 받아 러너에서 푼다.
*.jks
*.keystore
