import { SUPPORT_NOTICE_KO } from './supportNoticeText'

/**
 * 인트로 하단의 지원 표기 한 줄.
 *
 * ## 왜 인트로인가
 *
 * 앱에 설정 화면도 About 화면도 없다 — 개인정보처리방침 링크가 인트로 하단에 선 것과 같은
 * 사정이다 (`PrivacyNotice`). 표기를 둘 만한 "덜 방해되는 구석"이라는 것이 이 앱에는 없고,
 * 만들자고 화면 하나를 새로 세우면 아무도 열지 않는 화면에 규정 표기를 숨기는 꼴이 된다.
 *
 * 그래서 첫 화면이다. 제18조 2항이 요구하는 것은 대외에 드러나는 자리에 표시하는 것이고,
 * 이 앱에서 누구나 반드시 지나가는 화면은 인트로뿐이다. 스플래시는 몇 백 밀리초 뒤 사라져
 * 읽을 수 없으니 표시했다고 말하기 어렵다.
 *
 * ## 왜 웹 한 곳인가
 *
 * 인트로는 웹 SPA 화면이고 안드로이드·iOS는 그것을 WebView로 띄운다. 여기 한 줄을 두면
 * 안드로이드 앱·iOS 앱·브라우저 단독 웹 셋이 한꺼번에 해결된다 — 같은 문구를 세 런타임에
 * 세 번 적어 두고 규정이 바뀌는 날 한 곳만 고쳐지는 사고를 피한다.
 */
export function SupportNotice() {
  return <p className="type-caption support-notice">{SUPPORT_NOTICE_KO}</p>
}
