/**
 * 스토어 CTA의 문구 (사용자 요청 2026-09-15). `feedback/feedbackText.ts`·`ads/adConsentText.ts`와
 * 같은 자리다 — 화면과 테스트가 같은 값을 보게 하고, 문구를 고치는 사람이 JSX를 뒤지지 않게 한다.
 *
 * ## 왜 `storeLink.ts`가 아니라 따로인가
 *
 * 브라우저 E2E가 이 문구를 단언해야 하는데, 스펙은 **Node에서 돈다**. `e2e/tsconfig.json`의
 * `types`가 `["node"]`라 `import.meta.env`에 타입이 없고(의도된 설정이다 — 앱은 브라우저,
 * 스펙은 Node), `storeLink.ts`는 빌드 변수를 읽느라 그것을 쓴다. 거기 둔 문구를 스펙이
 * 가져오면 `tsc -p e2e`가 **앱 파일에서** 세 줄을 뱉는다(2026-09-15 실측).
 *
 * 문구만 떼면 스펙과 화면이 같은 상수를 보면서도 그 경계를 넘지 않는다. 베끼는 쪽은 택하지
 * 않았다 — 안내 문구가 두 벌이 되면 스토어에 올라간 날 한쪽만 지워진다.
 */

/**
 * 비활성 [앱 다운로드] 아래 한 줄. 버튼이 왜 눌리지 않는지 그 자리에서 답한다 —
 * 이유 없이 흐린 버튼은 고장으로 읽힌다.
 *
 * 한 상수를 두 화면(결과·마이크 차단)이 나눠 쓴다. 각자 적으면 한쪽만 고쳐져 같은 사정을
 * 다른 말로 안내하는 화면이 생긴다 — 스토어 URL을 `storeLink.ts` 한 곳에 모은 것과 같은 판단이다.
 *
 * 말투는 앱의 다른 카피와 같다 (ux-ui.md, "~해요"체). 사과로 시작하지 않는 것도 의도다 —
 * 등록을 기다리는 것은 사고가 아니라 예정된 일이고, 사용자가 받아야 할 정보는 "곧 나온다"다.
 */
export const STORE_PENDING_CAPTION = '앱 스토어 등록을 준비하고 있어요. 곧 만나요!'
