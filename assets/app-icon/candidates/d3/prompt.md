Use the built-in `image_gen` tool (default mode, no CLI fallback) to generate ONE image and save it as `icon-d3.png` in the current directory (overwrite). Do not ask questions; proceed with sensible defaults. Square 1024x1024.

One image is attached: our previous version of this icon. Keep the thick pitch contour exactly as it is — same shape, bumps, thickness, sticker margin, outline, colors and offset shadow. Change ONLY this: REMOVE the thin dashed guide line underneath completely. Nothing else on the paper but the single thick contour.

Prompt to send to image_gen:

Layered paper-cut app icon, flat front view, on one flat uniform cream paper background (#F3ECD9).
Subject: a voice pitch contour of one spoken sentence, drawn as one thick continuous smooth line from left to right, matching the attached image: starts mid-height, rises to a tall peak, falls, makes a smaller bump, dips low, then rises sharply at the very end — four bumps of clearly different heights, organic, never symmetric, never a simple sine wave, never resembling a letter. Rounded stroke ends. Stroke thickness about 1/9 of the canvas height. There is NO second line, NO dashed line, NO guide line — only this one contour.
Construction: the contour is a single piece of near-black card stock (#1C1A17) cut sticker-style with a thin uniform cream margin (#F3ECD9) and a bold black ink outline, laid on the cream paper, casting a hard offset shadow (#CFC5AA, no blur, toward the lower right). Slight visible paper thickness. Only these three colors, matte paper, no gradients.
Composition: the contour spans about 82% of the canvas width and about 65% of the height, centered; at least 9% empty cream margin on every side; nothing touches the edges.
Background: completely uniform cream (#F3ECD9). No axes, no grid, no dots, no numbers, no dashed line, no floor line, no gradient, no vignette, no texture, no lighting effects, no rounded-square icon frame. No text, no logo, no signature, no watermark anywhere.

After saving, print only: DONE icon-d3.png <width>x<height>.
