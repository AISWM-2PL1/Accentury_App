Use the built-in `image_gen` tool (default mode, no CLI fallback) to generate ONE image and save it as `icon-c2.png` in the current directory (overwrite). Do not ask questions; proceed with sensible defaults. Square 1024x1024.

One image is attached: our previous version of this icon. Keep the same three letters, order, paper-cut construction, colors and shadow. Change ONLY the letter proportions: the letters must be TALL — vertically elongated condensed glyphs, each letter about 1.6× taller than wide — so the row of three letters fills about 80% of the canvas HEIGHT and about 80% of the width.

Prompt to send to image_gen:

Layered paper-cut app icon, flat front view, on one flat uniform cream paper background (#F3ECD9).
Subject: exactly the three Korean consonant letters "ㅅㅌㄹ" (siot, tieut, rieul), in that order left to right, in one row. Very bold, TALL condensed rounded chunky display letterforms: each glyph is clearly taller than it is wide (about 1.6:1 height to width), with thick uniform strokes and rounded corners. Render the glyphs EXACTLY as "ㅅ", "ㅌ", "ㄹ" — no other characters, no syllable blocks, no Latin letters.
Construction: each letter is a separate piece of near-black card stock (#1C1A17) with a thin cream sticker margin (#F3ECD9) and a bold black ink outline around it, stamped onto the cream paper. Each piece casts a hard offset shadow (#CFC5AA, no blur, toward the lower right). Slight visible paper thickness. Only these three colors, matte paper, no gradients.
Composition: the three tall letters together fill about 80% of the canvas height and 80% of the width, centered, evenly spaced with small gaps; at least 8% empty cream margin on every side; nothing touches the edges.
Background: completely uniform cream (#F3ECD9). No floor line, no gradient, no vignette, no texture, no lighting effects, no rounded-square icon frame. No other text, no logo, no signature, no watermark anywhere.

After saving, print only: DONE icon-c2.png <width>x<height>.
